{
  "name": "taskbolt-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Serverless — no build step'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10",
    "jsonwebtoken": "^9.0",
    "bcryptjs": "^2.4"
  }
}
